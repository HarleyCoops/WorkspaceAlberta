{
  "mcpServers": {
    "notes": {
      "_comment": "Example: Local file-based notes server",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-notes"],
      "env": {}
    },
    "filesystem": {
      "_comment": "File system access - adjust paths as needed",
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-filesystem", "${WORKSPACE_PATH}"],
      "env": {}
    }
  },
  "_instructions": [
    "This is a template file. To use:",
    "1. Copy to your Cursor MCP settings location",
    "2. Replace ${VAR} placeholders with actual values",
    "3. Add additional servers as needed",
    "",
    "Environment variables are loaded from .env file"
  ]
}
