name: smoke
on: [push, pull_request]
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        if: ${{ hashFiles('requirements.txt', 'pyproject.toml', 'setup.py') != '' }}
        with:
          python-version: '3.x'
      - name: pip install
        if: ${{ hashFiles('requirements.txt') != '' }}
        run: pip install -r requirements.txt -r mcp-servers/canadabuys/requirements.txt
      - name: unit tests
        if: ${{ hashFiles('requirements.txt') != '' }}
        # Local-only suites; test_cohere_alberta and test_e2b_bid_room need
        # live API keys and are excluded on purpose.
        run: |
          python -m unittest -v \
            tests.test_procurement_http_app \
            tests.test_canadabuys_mcp_smoke \
            tests.test_procurement_fixtures \
            tests.test_search_relevance \
            tests.test_production_hardening \
            tests.test_telemetry \
            tests.test_box_source_guard \
            tests.test_box_mcp_handshake
      - name: pip install (pyproject)
        if: ${{ hashFiles('requirements.txt') == '' && hashFiles('pyproject.toml') != '' }}
        run: pip install .
      - uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - name: box unit tests
        run: node --test box/test/*.test.mjs
      - name: npm install + build
        if: ${{ hashFiles('package.json') != '' }}
        run: |
          npm ci || npm install
          npm run build --if-present
